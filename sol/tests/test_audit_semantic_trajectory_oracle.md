# `test_audit_semantic_trajectory_oracle.py`

## Purpose

Checks that the Gate 2a10 audit imports safely and that its frozen protocol exists.

## Coverage

- callable audit entry point;
- colocated semantic-trajectory protocol.
